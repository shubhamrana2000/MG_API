databases:
  - name: postgres
    ipAllowList: []
    databaseName: MG_API_render
    user: MG_API_render

services:
  - type: web
    name: MG_API_render
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false