<%= form_with(model: team) do |form| %>
  <% if team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
        <% team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :team_name, style: "display: block" %>
    <%= form.text_field :team_name %>
  </div>

  <div>
    <%= form.label :team_short_name, style: "display: block" %>
    <%= form.text_field :team_short_name %>
  </div>

  <div>
    <%= form.label :team_logo, style: "display: block" %>
    <%= form.text_field :team_logo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
